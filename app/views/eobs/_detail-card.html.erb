<div class='card bg-dark text-white'>
  <div class='card-body'>
    <table class='table table-striped table-dark status'>
      <colgroup>
        <col style="width: 15%"></col>
        <col style="width: 25%"></col>
        <col style="width: 40%"></col>
        <col style="width: 20%"></col>
      </colgroup>
      <thead>
        <tr> 
          <th>Date</th> 
          <th>Category</th>
          <th>P/S</th>
          <th>Location</th> 
        </tr> 
      </thead>
      </tbody>
        <% @eob.items.each do |item| %> 
          <tr class='eobitem'>
            <td><%= sanitize(item[:startDate]) %></td>
            <td><%= sanitize(item[:category].join(",")) %></td>
            <td>
              <p>
                <%= sanitize(item[:productOrService]) %>
              </p>
              <% if item[:adjudication].length > 0 %>
                <p>
                  Adjudication
                </p>
                <table class='table table-dark'>
                  <tbody>
                    <tr class='adjudication'>
                      <% item[:adjudication].each do |adj| %>
                        <td><%= sanitize(adj[1]) %></td>
                      <% end %>
                    </tr>
                    <tr class='adjudication'>
                      <% item[:adjudication].each do |adj| %>
                        <td><%= sanitize(adj[0]) %></td>
                      <% end %>
                    </tr>
                  </tbody>
                </table>
              <% end %>
            </td>
            <td><%= sanitize(item[:location].join(",")) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
